# pnpm 10/11 otherwise aborts `pnpm build` over esbuild's (unnecessary) install
# script and an interactive node_modules purge prompt. The Vite build works
# without esbuild's postinstall, so we skip the pre-run dependency check.
verify-deps-before-run=false
confirm-modules-purge=false
